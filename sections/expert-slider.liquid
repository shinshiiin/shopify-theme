<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/css/splide.min.css">

<div class="expert-slider-{{ section.id }}">
  <h2 class="section-title">{{ section.settings.section_title }}</h2>

  <div id="expert-splide-{{ section.id }}" class="splide">
    <div class="splide__track">
      <ul class="splide__list">
        
        {% for block in section.blocks %}
          <li class="splide__slide" {{ block.shopify_attributes }}>
            <div class="expert-card">
              <div class="image-wrapper">
                {% if block.settings.video_url != blank %}
                  <video 
                    class="expert-video" 
                    playsinline 
                    muted 
                    loop
                    poster="{% if block.settings.expert_image %}{{ block.settings.expert_image | img_url: '700x' }}{% else %}https://via.placeholder.com/700x500{% endif %}"
                  >
                    <source src="{{ block.settings.video_url }}" type="video/mp4">
                  </video>
                  <button class="unmute-button" data-video-index="{{ forloop.index }}">
                    <svg class="muted-icon" width="24" height="24" viewBox="0 0 24 24" fill="none">
                      <path d="M11 5L6 9H2v6h4l5 4V5zM19.07 4.93l-1.41 1.41A7.98 7.98 0 0 1 20 12a7.98 7.98 0 0 1-2.34 5.66l1.41 1.41A9.98 9.98 0 0 0 22 12c0-2.76-1.12-5.26-2.93-7.07zM16.24 7.76l-1.41 1.41A3.99 3.99 0 0 1 16 12c0 1.11-.45 2.11-1.17 2.83l1.41 1.41A5.99 5.99 0 0 0 18 12c0-1.66-.67-3.16-1.76-4.24z" fill="white"/>
                      <line x1="2" y1="2" x2="22" y2="22" stroke="white" stroke-width="2"/>
                    </svg>
                    <svg class="unmuted-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" style="display: none;">
                      <path d="M11 5L6 9H2v6h4l5 4V5zM19.07 4.93l-1.41 1.41A7.98 7.98 0 0 1 20 12a7.98 7.98 0 0 1-2.34 5.66l1.41 1.41A9.98 9.98 0 0 0 22 12c0-2.76-1.12-5.26-2.93-7.07zM16.24 7.76l-1.41 1.41A3.99 3.99 0 0 1 16 12c0 1.11-.45 2.11-1.17 2.83l1.41 1.41A5.99 5.99 0 0 0 18 12c0-1.66-.67-3.16-1.76-4.24z" fill="white"/>
                    </svg>
                  </button>
                  <button class="fullscreen-button">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                      <path d="M7 14H5v5h5v-2H7v-3zm-2-4h2V7h3V5H5v5zm12 7h-3v2h5v-5h-2v3zM14 5v2h3v3h2V5h-5z" fill="white"/>
                    </svg>
                  </button>
                {% else %}
                  <img src="{% if block.settings.expert_image %}{{ block.settings.expert_image | img_url: '700x' }}{% else %}https://via.placeholder.com/700x500{% endif %}" class="expert-image" alt="{{ block.settings.expert_name }}">
                {% endif %}
              </div>
              <div class="expert-info">
                <h3>{{ block.settings.expert_name }}</h3>
                <p class="expert-role">{{ block.settings.expert_role }}</p>
                <p class="expert-desc">{{ block.settings.expert_description }}</p>
              </div>
            </div>
          </li>
        {% endfor %}

      </ul>
    </div>

    <div class="splide__arrows">
      <button class="splide__arrow splide__arrow--prev">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
          <path d="M15 18L9 12L15 6" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </button>
      <button class="splide__arrow splide__arrow--next">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
          <path d="M9 18L15 12L9 6" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </button>
    </div>
  </div>
</div>

<div class="video-modal-overlay" id="ExpertVideoModal">
  <div class="video-modal-content">
    <button class="close-modal">✕</button>
    <video id="modalVideo" controls width="100%" height="auto">
      <source src="" type="video/mp4">
    </video>
  </div>
</div>


{%- style -%}
  .expert-slider-{{ section.id }} {
    padding: 60px 20px;
    background: #1a1a1a;
    font-family: sans-serif;
    overflow: hidden;
  }

  .expert-slider-{{ section.id }} .section-title {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 50px;
    text-align: center;
    color: #fff;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .expert-slider-{{ section.id }} .splide {
    max-width: 1400px;
    margin: 0 auto;
    position: relative;
    padding: 0 60px;
  }

  .expert-slider-{{ section.id }} .expert-card {
    background: #fff;
    border-radius: 16px;
    overflow: hidden;
    height: 100%;
  }

  .expert-slider-{{ section.id }} .image-wrapper {
    position: relative;
    width: 100%;
    line-height: 0;
    background: #d3d3d3;
  }

  .expert-slider-{{ section.id }} .expert-image,
  .expert-slider-{{ section.id }} .expert-video {
    width: 100%;
    height: 280px;
    object-fit: cover;
    display: block;
  }

  .expert-slider-{{ section.id }} .expert-video {
    background: #000;
  }

  .expert-slider-{{ section.id }} .unmute-button {
    position: absolute;
    bottom: 16px;
    left: 16px;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.7);
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    z-index: 2;
  }

  .expert-slider-{{ section.id }} .unmute-button:hover {
    background: rgba(0, 0, 0, 0.85);
    transform: scale(1.1);
  }

  .expert-slider-{{ section.id }} .fullscreen-button {
    position: absolute;
    bottom: 16px;
    right: 16px;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.7);
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    z-index: 2;
  }

  .expert-slider-{{ section.id }} .fullscreen-button:hover {
    background: rgba(0, 0, 0, 0.85);
    transform: scale(1.1);
  }

  .expert-slider-{{ section.id }} .expert-info {
    padding: 20px;
    background: #fff;
  }

  .expert-slider-{{ section.id }} .expert-info h3 {
    font-size: 1.25rem;
    margin: 0 0 8px 0;
    font-weight: 700;
    color: #000;
  }

  .expert-slider-{{ section.id }} .expert-role {
    text-transform: uppercase;
    font-size: 0.7rem;
    color: #666;
    letter-spacing: 1px;
    margin-bottom: 12px;
    font-weight: 600;
  }

  .expert-slider-{{ section.id }} .expert-desc {
    font-size: 0.95rem;
    color: #444;
    line-height: 1.5;
  }

  /* Arrow Styles */
  .expert-slider-{{ section.id }} .splide__arrow {
    width: 48px;
    height: 48px;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.1);
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .expert-slider-{{ section.id }} .splide__arrow:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: scale(1.05);
  }

  .expert-slider-{{ section.id }} .splide__arrow--prev {
    left: 0;
  }

  .expert-slider-{{ section.id }} .splide__arrow--next {
    right: 0;
  }

  .expert-slider-{{ section.id }} .splide__arrow:disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }

  /* Modal Styles */
  .video-modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.9);
    display: none;
    justify-content: center;
    align-items: center;
    z-index: 9999;
  }

  .video-modal-content {
    position: relative;
    width: 90%;
    max-width: 900px;
    background: #000;
    border-radius: 15px;
    overflow: hidden;
  }

  .video-modal-overlay.active {
    display: flex;
  }

  .close-modal {
    position: absolute;
    top: 15px;
    right: 15px;
    background: rgba(255, 255, 255, 0.9);
    border-radius: 50%;
    width: 35px;
    height: 35px;
    border: none;
    cursor: pointer;
    z-index: 10;
    font-size: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .close-modal:hover {
    background: #fff;
  }

  @media (max-width: 1024px) {
    .expert-slider-{{ section.id }} .splide {
      padding: 0 50px;
    }
  }

  @media (max-width: 768px) {
    .expert-slider-{{ section.id }} .section-title {
      font-size: 1.5rem;
    }

    .expert-slider-{{ section.id }} .splide {
      padding: 0 40px;
    }

    .expert-slider-{{ section.id }} .splide__arrow {
      width: 40px;
      height: 40px;
    }
  }

  @media (max-width: 640px) {
    .expert-slider-{{ section.id }} .splide {
      padding: 0 35px;
    }
  }
{%- endstyle -%}

<script src="https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/js/splide.min.js"></script>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    // Initialize Slider with infinite loop
    const splide = new Splide('#expert-splide-{{ section.id }}', {
      type   : 'loop',
      perPage: 4,
      gap    : '20px',
      pagination: false,
      autoplay: false,
      pauseOnHover: true,
      breakpoints: {
        1200: { perPage: 3 },
        900: { perPage: 2 },
        640:  { perPage: 1 }
      }
    }).mount();

    // Video autoplay with Intersection Observer
    const videos = document.querySelectorAll('.expert-slider-{{ section.id }} .expert-video');
    
    const observerOptions = {
      root: null,
      rootMargin: '0px',
      threshold: 0.5
    };

    const videoObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        const video = entry.target;
        if (entry.isIntersecting) {
          video.play().catch(e => console.log('Autoplay prevented:', e));
        } else {
          video.pause();
        }
      });
    }, observerOptions);

    videos.forEach(video => {
      videoObserver.observe(video);
    });

    // Unmute/Mute toggle
    document.querySelectorAll('.unmute-button').forEach(btn => {
      btn.addEventListener('click', function(e) {
        e.stopPropagation();
        const card = this.closest('.expert-card');
        const video = card.querySelector('.expert-video');
        const mutedIcon = this.querySelector('.muted-icon');
        const unmutedIcon = this.querySelector('.unmuted-icon');
        
        if (video.muted) {
          video.muted = false;
          mutedIcon.style.display = 'none';
          unmutedIcon.style.display = 'block';
        } else {
          video.muted = true;
          mutedIcon.style.display = 'block';
          unmutedIcon.style.display = 'none';
        }
      });
    });

    // Fullscreen modal
    const modal = document.getElementById('ExpertVideoModal');
    const modalVideo = document.getElementById('modalVideo');
    const closeBtn = modal.querySelector('.close-modal');

    document.querySelectorAll('.fullscreen-button').forEach(btn => {
      btn.addEventListener('click', function(e) {
        e.stopPropagation();
        const card = this.closest('.expert-card');
        const video = card.querySelector('.expert-video');
        const videoSrc = video.querySelector('source').src;
        
        // Pause all videos in slider
        videos.forEach(v => v.pause());
        
        modalVideo.querySelector('source').src = videoSrc;
        modalVideo.load();
        modal.classList.add('active');
        modalVideo.play();
      });
    });

    const closeModal = () => {
      modal.classList.remove('active');
      modalVideo.pause();
      
      // Resume playing videos that are in view
      videos.forEach(video => {
        const rect = video.getBoundingClientRect();
        const isInView = rect.top >= 0 && rect.bottom <= window.innerHeight;
        if (isInView) {
          video.play().catch(e => console.log('Autoplay prevented:', e));
        }
      });
    };

    closeBtn.addEventListener('click', closeModal);
    modal.addEventListener('click', (e) => {
      if(e.target === modal) closeModal();
    });
  });
</script>

{% schema %}
{
  "name": "Expert Video Slider",
  "settings": [
    {
      "type": "text",
      "id": "section_title",
      "label": "Section Title",
      "default": "OVER 5000+ SUCCESS STORIES"
    }
  ],
  "blocks": [
    {
      "type": "expert",
      "name": "Expert",
      "settings": [
        {
          "type": "image_picker",
          "id": "expert_image",
          "label": "Expert Image/Poster",
          "info": "Used as video poster/thumbnail"
        },
        {
          "type": "text",
          "id": "expert_name",
          "label": "Expert Name",
          "default": "Dr. Name"
        },
        {
          "type": "text",
          "id": "expert_role",
          "label": "Expert Role",
          "default": "Board-certified dermatologist"
        },
        {
          "type": "textarea",
          "id": "expert_description",
          "label": "Expert Description",
          "default": "Description of what the expert explains in the video."
        },
        {
          "type": "url",
          "id": "video_url",
          "label": "Video URL",
          "info": "Direct link to the MP4 video file"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Expert Video Slider",
      "blocks": [
        {
          "type": "expert",
          "settings": {
            "expert_name": "Dr. Andrea Suarez",
            "expert_role": "Board-certified dermatologist",
            "expert_description": "Explains why the iRESTORE Elite is the most powerful device and why she recommends it to her patients."
          }
        },
        {
          "type": "expert",
          "settings": {
            "expert_name": "Dr. Adam Bodian",
            "expert_role": "Board-Certified Laser Surgeon",
            "expert_description": "Reports on how 100% of device clinical study patients experienced hair growth."
          }
        },
        {
          "type": "expert",
          "settings": {
            "expert_name": "Dr. Eshini Perera",
            "expert_role": "Board-certified dermatologist",
            "expert_description": "Explains how low-level light therapy (LLLT) works to stimulate hair growth."
          }
        },
        {
          "type": "expert",
          "settings": {
            "expert_name": "Dr. Andrea Suarez",
            "expert_role": "Board-certified dermatologist",
            "expert_description": "Explains why the iRESTORE Elite is the most powerful device and why she recommends it to her patients."
          }
        }
      ]
    }
  ]
}
{% endschema %}
